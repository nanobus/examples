spec: apex.axdl
config:
  name: test_project
plugins:
  - 'https://deno.land/x/iota_codegen@v0.0.3/rust/plugin.ts'
tasks:
  debug:
    deps:
      - generate
    cmds:
      - cargo build --target=wasm32-wasi
      - mkdir -p build
      - cp ./target/wasm32-wasi/debug/test_project.wasm build/
